---
description: 乳腺癌多器官转移批量转录组/蛋白组：神经浸润基因与继发部位倾向
globs: "**/*.{R,r,Rmd}"
alwaysApply: true
---

# 乳腺癌转移 bulk：原位肺倾向 + 神经浸润

仅当任务是**转移灶/原发灶批量转录组或蛋白组**时按本规则。单细胞 / 空间分别用 `brca-met-scrna-nerve.mdc`、`brca-met-st-nerve.mdc`。不要套用 NTC/TG_sh Cuffdiff，不要改 `TG_RNAseq_*.R`。

公开、可下载、多病人、含肺/脑/骨信息的人源数据以转录组为主。禁止小鼠 PDX 当病人。

## 当前主问题（原位灶肺倾向）——优先用此脚本

脚本：`Nerve_RNA_primary_lung_tropism.R`（目录 `E:/R/Nerve RNA`）

```r
setwd("E:/R/Nerve RNA")
source("Nerve_RNA_primary_lung_tropism.R")
```

数据：

1. **GSE2603**（MSK 原发灶，GPL96）——主队列；`lm event` / `met event` / `bm event`
2. **GSE5327**（Erasmus ER- 原发灶）——验证/合并；`lung met all` / `metastasis`
3. **GPL96.annot.gz**——探针→基因
4. 可选蛋白组：`proteomics_pg_matrix.csv` + `proteomics_sample_meta.csv`

### 两个问题必须按此定义

1. **倾向肺转移的原位组织 vs 倾向其他器官转移的原位组织 → 下调基因**
   - **必须对应**：原位灶与转移器官结局为同一患者临床随访（`lm event` / `lung met all`），写出 `00_SAMPLE_CORRESPONDENCE_primary_to_met_organ.csv`
   - `lung_tropic` = 该患者原发 + 确实发生肺转移；`other_met` = 该患者原发 + 转移但无肺转移
   - **禁止**用 LMS 签名、无结局样本、细胞系、转移灶活检冒充原位
   - 先 **p < 0.01**，再只取 **FC < 1**；**不要**做 1.25 / 1.5 / 2 分层
   - 必须出火山图 + Excel（`01_*_DOWN_FC_lt1.xlsx`）
2. **在肺倾向原位组织内，与神经浸润负相关的蛋白**
   - **必须用问题1同一批**有肺转移临床对应的 `lung_tropic` 原位灶
   - 先算神经浸润签名分数，再 Spearman；取 **rho < 0 且 p < 0.01**
   - 无匹配公开蛋白组时用 RNA 丰度作蛋白代理，并在表中标注 `RNA_proxy_for_protein`
   - 有用户蛋白矩阵时优先用蛋白

结果目录：`results_primary_lung_tropism/`。

```r
# ❌ BAD: 用 LMS 签名或转移灶 GSE175692 冒充「肺转移对应的原位灶」
# ❌ BAD: 问题1做 FC≥1.25/1.5/2 多层；Q2 用与 Q1 不同的样本
# ✅ GOOD: 同一患者临床结局对应；p<0.01 且 FC<1；Q2 样本 ⊆ Q1 lung_tropic
```

## 旧脚本（转移灶 GSE175692）仍保留

`Nerve_RNA_nerve_infiltration.R`：转移灶活检上的配体高表达 / 部位排名 / 屏障低表达。与「原位肺倾向」问题不同，不要混用。
