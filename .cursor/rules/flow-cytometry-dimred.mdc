---
description: 流式 EV vs H 三 panel 降维（P1/P2/P3，unmixed FCS，导出 UMAP/tSNE 图）
globs: "**/Flow*.{R,r,Rmd}"
alwaysApply: false
---

# 流式 H vs EV：降维与免疫细胞比较

写或改流式分析时按本规则。输入是 **unmixed FCS**，不是 raw、不是 Cuffdiff。RNA-seq 仍走 `TG_RNAseq_*.R`。

## 数据

- 目录：`E:/R/flow J-LJY WJZ ZZX`（脚本与结果同目录；缺失则回退 `E:/R/flow J` / 仓库根 / `flow_J`）
- 只用 `*_unmixed.fcs`，不要拿 raw 做降维
- 文件名：`ZZX_EV1-1_P1_unmixed.fcs` / `EV1-1_P1_unmixed.fcs`（`ZZX_` 可有可无；生物学重复 1/2/3，技术重复 `-1`/`-2`）。旧名 `EV1_P1_unmixed.fcs` 仍可用。**先匹配 EV 再匹配 H**
- 样品：`ZZX_EV` = EV1/EV2/EV3，每管两个技术重复 `EV1-1` `EV1-2`；`ZZX_H` = H1/H2/H3，同样 `H1-1` `H1-2`。圈门按 **FCS 管（技术重复）** 单独做；H vs EV 统计先把两个技术重复平均，再按 **n=3 生物学重复** 做 t 检验，不要把 6 个技术管当成 n=6
- 三个 panel **分开分析、分开出图**，禁止把 P1/P2/P3 拼成一张表达矩阵。全亚群总览只汇总各 panel 已圈好的**管内频率**（`Flow_dimred_all_subsets.R` → `results_flow/all_subsets/`），百分数不能跨 panel 加总，也不能拿去再做联合 UMAP
- 细胞轨迹（`Flow_dimred_trajectory.R`）按 **panel × 大类** 单独做：P1 的 CD4/CD8，P2 的 B，P3 的髓系。类内 PCA 当 Component 1/2，骨架用 slingshot（若已装）或亚群中心 MST。根节点按惯例取 naive / Naive B / Ly6C hi 单核。轨迹是标志物状态连续体，不要当成发育起源证明。出图：细胞点要够大、黑色分支骨架要细，先画线再画点，不要圈很小、线很粗
- **`skip trajectory (... subsets= 1)` 不是失败。** 树至少要 2 个亚群节点。P1 的 NK/NKT 门控后经常整群只有一个标签，日志会写 `only 1 subset, no tree to draw`。真正要看的是 P1 CD4/CD8、P2 B、P3 Myeloid。`Panel Px failed: missing value where TRUE/FALSE needed` 才是 bug：`parent_mask` / `if (sum(par) < 20)` 遇到 NA 会炸；比较必须把 NA 收成 FALSE，subset 图失败不得炸掉整块 panel
- 物种：小鼠（NK1.1、F4/80、I-A/I-E、Ly6C/G、Siglec-F、Sca-1）
- 荧光素→标志物以 `flow_panel_map.json` / `Flow_dimred_pipeline.R` 为准。Cytek unmixed 通道名常是 `BUV496-A`、`APC-Cy7-A`（带 Area 后缀）或 `CD45 V500`；匹配时要去掉 `-A/-H/-W`、按 token 认标志物/荧光素，不要要求整串 equal。对不上时必须把 `$PnN`/`$PnS` 打进日志，不要只报「没有匹配到任何分析通道」
- **NK1.1 抗体是 AF700**，Cytek unmixed 通道名经常仍是 `R718-A` / `APC-R700-A`（同一 ~700 nm 检测器），两种都要配上，不能只认字面 `AF700-A`
- Windows 若把 json 存成“文本文档”，真实名常是 `flow_panel_map.json.txt`：脚本要认这个名字，不要只找 `.json`

## 预处理（每个 panel 六个样品一起做一次，再降维）

1. 去碎片 / 双联体（有 FSC/SSC 时）；L/D(FVS450) 去死；CD45+ 白细胞
2. 荧光通道 asinh（流式 cofactor 约 150，不要用 CyTOF 的 5）
3. L/D 只用于 QC，**不要**作为 UMAP/tSNE 特征；CD45 门控后默认可排除
4. 按样品等量下采样再联合 embedding，避免大文件主导；频率统计用 QC 后细胞，不要只用 DR 子集冒充频率
5. 不要用未门控、未转换的 raw 强度做降维

## 比较与出图（每 panel 必须单独出）

比较只有 **H vs EV**（三个生物学重复 vs 三个；技术重复先平均）。联合 UMAP/tSNE 仍用全部细胞，用 `group`/`sample` 着色，不要先把重复平均掉再降维。

每个非空 panel 必须导出 PDF+PNG，其中主图是左右分面：左 EV、右 H。主图 `*_H_vs_EV_tSNE_lineage_split` / `*_UMAP_lineage_split` **按细胞亚群分区**，每块画该亚群的**填充密度区域**，不要把每个细胞画成散点，也不要虚线凸包。联合散点图仍出 `*_UMAP_by_lineage` 和 `*_lineage_split_joint`。

**圈门必须每个样品单独走完同一套逻辑**（`hierarchical_gate_by_sample`），不要六个 FCS 共用一个 kmeans 阈值。CD62L/CD44 用完整象限：naive = 62L+44−，TCM = 双阳，TEM = **整个 CD62L− 半平面**（CD44+ 与原点双阴都算 TEM，不要只框 10–20% 的小矩形）。阳性阈值要切在原点主团右侧（抓住 CD62L/CD44 尾巴），不要用全体 q80 把主团切开。每个 FCS 另出 `gating/<样品>/`：2D 十字门铺满坐标轴，CD62L/CD44 标四个象限百分数。subset 图的 EV、H 也各自按本组细胞切完整门，禁止用命中细胞的 10–90% 分位数小框。

**每个亚群**还要出一组论文风格图（`subset_stats/`）：上方 EV（黑）vs H（红）柱状图（均值、**SD** 误差棒、每个重复的白心点、显著性 `ns/*/**/*** `；ns 时标出 P 值）。n=3 时柱高看起来不同也会 ns，不要为了出星号改检验。下方 2D 图按 FlowJo：**概率等高线 + 最外圈以外的 outliers**，EV/H 共用坐标轴、同一门框和百分数。不要只用 ggplot `stat_density_2d(ndensity)`（那是峰值等高线，看起来不像文章里的流式图）。

- P1：**先圈大类**（B / 髓系 / NK / NKT / **CD4** / **CD8**）。T 细胞一律判到 CD4 或 CD8，不要输出 T TEM。CD4 内再一层层圈：CD25→Treg，CD69/TNF-a→activated，剩余只用 CD62L vs CD44 分成 naive/TCM/TEM。CD8 内：LAG-3/TIM-3→exhausted，GZMB/Perforin→effector，剩余 naive/TCM/TEM。
- P2：**先圈大类**（IgD vs CD27 的 **两个团互相比较** CD27−IgD，CD27 相对更高的一团是 Memory；**不要**要求同一细胞 CD27 绝对值压过 IgD，IgD 背景会把全部打成 Naive）。**不要把 BLIMP-1 放进第 1 层**。Memory 内再一层层圈：BLIMP-1（两群分得开、高于 IgD、且不是多数派才叫 Plasma）→ **少数** CD80/CD86 岛才叫 Activated（**不要用 CD40**，静息 B 也有 CD40；k=2 对单峰 CD80 会把一半吞掉）→ IgG→Switched → 剩余 IgM→IgM memory / Memory。Naive 内同样：只用 CD80/CD86 圈少数 Activated，且必须明显高于 IgD；剩余 IgD vs IgM → Naive / Atypical。默认剩余是 Naive/Memory，不是 Plasma，也不是 Activated。
- P3：**先圈大类**（CD3/CD19/NK1.1 vs 髓系）。髓系内**一层层圈**，不要把 Ly6G/Siglec-F/F4/80/CD11c/Ly6C 丢进一次 kmeans 比大小：Ly6G→中性粒（Ly6C 中阳仍是中性粒），Siglec-F→嗜酸（不用 CCR3），FceRI/CD200R3→嗜碱，F4/80（须高于 CD11c，不用 MHCII 抢赢）→巨噬，CD103→CD103 DC，CD11c→DC，剩余 Ly6C hi/lo 单核。巨噬圈定后再用 iNOS→M1、CD206/ARG-1→M2；不用 IL-10/TGF-b/TNF-a/CD86 当极化。
- 类内 kmeans 只跑该层标志。不要全局 kmeans 后整团贴一个名字，也不要按 rank 在 UMAP 上撒点
- **P1/P2/P3 共用 P1 那套高对比色**（红/紫/蓝/米/酒红/金/绿/橙）。禁止 P2 全紫、P3 全橙。组别色：EV 黑、H 红

```r
# ❌ BAD: 用 raw FCS；把 EV1 解析成 H；三 panel 混降维；只出坐标表不出图
# ❌ BAD: 把 EV1/EV2/EV3 先平均成一个“EV”再 UMAP
# ❌ BAD: 只出 UMAP，不出每个亚群的柱状图+等高线
# ✅ GOOD: 仅 unmixed；P1/P2/P3 各自目录；联合 embedding + 每图 PDF/PNG + subset_stats/
```
