---
description: 流式 T vs T6 三 panel 降维（P1/P2/P3，unmixed FCS，导出 UMAP/tSNE 图）
globs: "**/Flow*.{R,r,Rmd}"
alwaysApply: false
---

# 流式 T6 vs T：降维与免疫细胞比较

写或改流式分析时按本规则。输入是 **unmixed FCS**，不是 raw、不是 Cuffdiff。RNA-seq 仍走 `TG_RNAseq_*.R`。

## 数据

- 目录：`E:/R/flow J`（脚本与结果同目录；缺失则回退仓库根 / `flow_J`）
- 只用 `*_unmixed.fcs`，不要拿 raw 做降维
- 文件名：`{T|T6}-{1|2|3}_P{1|2|3}_unmixed.fcs`；**先匹配 T6 再匹配 T**，禁止把 `T6-1` 当成 T
- 六个样品独立保留：`T-1` `T-2` `T-3` `T6-1` `T6-2` `T6-3`
- 三个 panel **分开分析、分开出图**，禁止把 P1/P2/P3 拼成一张表达矩阵
- 物种：小鼠（NK1.1、F4/80、I-A/I-E、Ly6C/G、Siglec-F、Sca-1）
- 荧光素→标志物以 `flow_panel_map.json` / `Flow_dimred_pipeline.R` 为准。Cytek unmixed 通道名常是 `BUV496-A`、`APC-Cy7-A`（带 Area 后缀）或 `CD45 V500`；匹配时要去掉 `-A/-H/-W`、按 token 认标志物/荧光素，不要要求整串 equal。对不上时必须把 `$PnN`/`$PnS` 打进日志，不要只报「没有匹配到任何分析通道」
- Windows 若把 json 存成“文本文档”，真实名常是 `flow_panel_map.json.txt`：脚本要认这个名字，不要只找 `.json`

## 预处理（每个 panel 六个样品一起做一次，再降维）

1. 去碎片 / 双联体（有 FSC/SSC 时）；L/D(FVS450) 去死；CD45+ 白细胞
2. 荧光通道 asinh（流式 cofactor 约 150，不要用 CyTOF 的 5）
3. L/D 只用于 QC，**不要**作为 UMAP/tSNE 特征；CD45 门控后默认可排除
4. 按样品等量下采样再联合 embedding，避免大文件主导；频率统计用 QC 后细胞，不要只用 DR 子集冒充频率
5. 不要用未门控、未转换的 raw 强度做降维

## 比较与出图（每 panel 必须单独出）

比较只有 **T6 vs T**（三 vs 三）。联合 UMAP/tSNE，用 `group`/`sample` 着色，不要先把重复平均掉再降维。

每个非空 panel 必须导出 PDF+PNG，其中主图是左右分面：左 T、右 T6，点按**细亚群**着色，不要虚线凸包。主图 `*_T_vs_T6_tSNE_lineage_split`。

- P1：**先圈大类**（B / 髓系 / NK / NKT / CD4 / CD8，只用谱系抗体），**大类圈定后再分亚群**（CD4 用 CD25、CD69、TNF-a、CD62L、CD44；CD8 用 LAG-3、TIM-3、GZMB、Perforin、CD62L、CD44）。类内 kmeans 只跑这一层的标志，再按亚群中位数命名。不要全局 kmeans 后整团贴一个 TEM，也不要按 rank 在 UMAP 上撒点。
- P2：先圈 B，再在 B 内用 IgD/IgM/IgG/CD27/BLIMP-1/CD80·CD86 分 Naive / IgM memory / Memory / Switched / Activated / Plasma
- P3：先丢掉 T/B/NK，再在髓系内用 Ly6G、Siglec-F、F4/80、iNOS、CD206、CD11c、CD103、Ly6C、FceRI 分亚群（不用 CCR3 当嗜酸，不用 IL-10/TGF-b 当 M2）
- P2：cluster 内 IgD/IgM/IgG/CD27/BLIMP-1，默认 Naive B，不要大量 other
- P3：cluster 内 Siglec-F（不要用 CCR3 当嗜酸）、Ly6G、F4/80、iNOS、CD206、CD11c/MHCII、CD103、Ly6C、FceRI；淋巴细胞污染标 T/B/NK；平局标 Myeloid / Ly6C lo，不要默认 other，也不要把泛 CD11B 当成一个亚群去抢赢
- **P1/P2/P3 共用 P1 那套高对比色**（红/紫/蓝/米/酒红/金/绿/橙）。禁止 P2 全紫、P3 全橙

```r
# ❌ BAD: 用 raw FCS；把 T6-1 解析成 T；三 panel 混降维；只出坐标表不出图
# ❌ BAD: 把 T-1/T-2/T-3 先平均成一个“T”再 UMAP
# ✅ GOOD: 仅 unmixed；P1/P2/P3 各自目录；联合 embedding + 每图 PDF/PNG
```
