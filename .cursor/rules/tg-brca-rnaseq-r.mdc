---
description: TG-BRCA RNA-seq 的 R 实现：Cuffdiff 读入、作图、ORA 与 GSEA 分工、输出目录
globs: "**/*.{R,r,Rmd}"
alwaysApply: false
---

# R 实现细节

主脚本：`R/analyze_tg_rnaseq.R`（**自带默认配置**）。找不到 `00_config.R` 时用内置默认值，数据目录默认 `E:/R/TG_BRCA/TG`。交互粘贴或 `source()` 后必须自动跑 `main()`。

热图/火山标注只用**单个官方 gene symbol**。Cuffdiff 的 `XLOC_*` 和 `MIR6516,SCARNA16` 这类逗号名：完整表可保留，作图和 GO/KEGG/GSEA 映射前必须拆开并丢掉 XLOC。热图还要求至少一组 FPKM ≥ `MIN_FPKM`（默认 1）。`inf` log2FC 用 `log2((v2+0.1)/(v1+0.1))` 重算。

```r
# ❌ BAD：rownames 留 XLOC_020085 或 "MIR6516,SCARNA16"
# ✅ GOOD
gene <- primary_gene_symbol(raw)   # MIR6516
pheatmap(log2(fpkm[annotated, c("NTC", "TG_sh1", "TG_sh5")] + 1), scale = "row")
```

- 成对比较：火山图 x=log2FC、y=-log10(p)。
- 共同上调：sh1 vs sh5 log2FC 散点图。
- **GSEA 必须出图**（`gseaplot_*.pdf/png`、dotplot）。`pvalueCutoff=1` 保留全部通路再画；显著子集另存 `*_significant.csv`。失败写真实错误，禁止只留 1KB 空表。

## 输出

`results/<比较>/<筛选>/`：`sh1_vs_NTC`、`sh5_vs_NTC`、`meanSH_vs_NTC`、`common_up_sh1_sh5`。
