---
description: TCGA-BRCA 中每个 GO 通路单独做临床相关与负相关基因分析（禁止合并通路）
globs:
  - "**/*.R"
  - "**/*.r"
  - "**/*.Rmd"
alwaysApply: false
---

# 每个 GO 通路单独分析（禁止合并）

分析 TCGA-BRCA 的 GO 通路时，**必须对用户列出的每一个 GO ID 单独循环**。禁止把多个 GO 基因集合并成一条“综合通路”再算相关。

**不要使用** `GSVA`、`AnnotationDbi`、`org.Hs.eg.db`。通路活性用 **combined z-score**（该 GO 内基因在样本间 z-score 后取均值）。GO 基因用本地 `data/go_genesets.tsv`，或 QuickGO / NCBI `gene2go`（含子术语，等价 GOALL）。只依赖 CRAN：`data.table`、`survival`。

## 数据（只读这些输入）

- `TCGA-BRCA.clinical`
- `TCGA-BRCA.protein`
- `TCGA-BRCA.star_fpkm`
- `TCGA-BRCA.survival`
- `gencode.v36.annotation.gtf.gene`

表达用 `log2(FPKM+1)`；样本优先肿瘤 `01`，再按条形码前 12 位匹配临床/生存。无基因或匹配后 < 5 个基因的 GO 跳过并记录，不要并入其他通路。

## 固定 GO 列表（逐个分析，不可合并）

`GO:0023041`, `GO:1904457`, `GO:1904340`, `GO:2001224`, `GO:2001222`, `GO:0019227`, `GO:0019228`, `GO:1902847`, `GO:0031102`, `GO:0097492`, `GO:0097491`, `GO:0097374`, `GO:0007158`, `GO:1902667`, `GO:0031103`, `GO:0007411`, `GO:0007409`

## 任务 1：每个通路 vs 临床（不是所有通路一起）

对 **每一个** GO：先算该通路自己的样本活性向量，再单独与分期、亚型、年龄、OS 等做相关。结果必须带 `GO_ID`，每个通路单独输出。

```r
# ❌ BAD：合并所有 GO；也不要用 GSVA / AnnotationDbi
combined <- unique(unlist(go_genesets))
score <- colMeans(expr[combined, ])

# ✅ GOOD：每个 GO 单独 z-score 打分、单独相关
z <- t(scale(t(expr)))
for (go_id in go_ids) {
  score_i <- colMeans(z[intersect(go_genesets[[go_id]], rownames(z)), , drop = FALSE])
  cor.test(score_i[samples], age[samples], method = "spearman")
  coxph(Surv(time, event) ~ score_i, data = surv_df)
}
```

## 任务 2：每个通路的负相关基因（不是所有通路一起）

对 **每一个** GO：用该通路自己的活性向量对全基因组做 Spearman；只保留 `rho < 0` 且 FDR < 0.05。禁止合并通路分数，禁止把 17 个通路的负相关基因取并集/交集当成“每个通路”的结果。

```r
# ❌ BAD
rho <- cor(expr, combined_score, method = "spearman")

# ✅ GOOD
for (go_id in go_ids) {
  score_i <- pathway_scores[go_id, samples]
  rho <- cor(t(expr[, samples]), score_i, method = "spearman")
  neg <- which(rho < 0 & p.adjust(pval, "BH") < 0.05)
  write.csv(result, paste0("negcorr_", go_id, ".csv"))
}
```
