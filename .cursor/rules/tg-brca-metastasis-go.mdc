---
description: 列出 GO 的气泡图用全基因组 enrichGO 统计，分别出前15和前20，y 轴 GeneRatio 最大在上
globs: "**/*.{R,r,Rmd}"
alwaysApply: true
---

# 列出的 GO 通路：全库统计 + 通路表达

`metastasis_custom_genes.txt` 是 **GO 通路名称 + GO 号**。通路表达（基因热图、ssGSEA/mean z）按每条 GO 单独做。气泡图**不要**只在自选通路上做超几何再 BH。

## 气泡图（每个非空子集）

1. 对该子集基因跑标准 `clusterProfiler::enrichGO`（BP/MF/CC；BH 在全库条目上校正；默认 minGSSize=10、maxGSSize=500）
2. 用全库结果里的 **GeneRatio、p.adjust、Count** 抽出列出的 GO ID；**禁止**对抽出的子集重新 `p.adjust`
3. **按 GeneRatio 从大到小**分别取前 15 与前 20，各出一张气泡图；最大的在最上面。top20 = top15 再在下面加 5 条，不要按 p.adjust 另选一套再重排
4. 全库表与气泡图写 `GO/*_ORA_GO_BP_dotplot_top15.pdf` 等；抽出的列出通路写 `CustomGO/`，文件名以 `ORA_` 开头

这样 `mitochondrion organization` 的 p.adjust 与正常 GO BP 图一致，而不是只在 ~70 条自选通路上重算后虚高。

```r
# ❌ BAD: 按 p.adjust 取 topN，再按 GeneRatio 画图（top15 和 top20 最上面会换通路）
# ✅ GOOD: for (n in c(15, 20)) plot(head(hit[order(-GeneRatio)], n))  # 20 只在下面多 5 条
```
