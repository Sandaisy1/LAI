---
description: 用 metastasis_custom_genes.txt 的 GO 通路分析 RNA-seq 表达（基因热图、通路分数、ORA 气泡图）
globs: "**/*.{R,r,Rmd}"
alwaysApply: true
---

# 列出的 GO 通路：看表达，不是全库富集

`metastasis_custom_genes.txt` 是 **GO 通路名称 + GO 号**，不是基因符号列表。用 `org.Hs.eg.db` 的 `GOALL` 把每条 GO 映射成基因，再在 RNA-seq 里看这些通路的表达。

## 输入

- `E:/R/TG_BRCA/TG/metastasis_custom_genes.txt`
- 每行一条通路，含 `GO:#######` 或 `GO_#######`；空行与 `#` 忽略
- 解析不到任何 GO 号就停止，不要退回成“按基因符号做 GSVA”

## 必须输出

对六组比较的每个非空子集（p0.05/p0.01 的 FC 与 topN，以及 AllDE）：

- `CustomGO/` 下 ORA 表 + **气泡图**（x=GeneRatio，size=Count，color=p.adjust；点出全部列出的 GO，不要只留显著通路）
- 子集基因中落在列出 GO 里的热图

另外（比较级 / 全样品）：

- `00_PathwayExpression/`：每条 GO 单独的基因热图 + 通路分数热图（ssGSEA 或 mean z）
- 各比较的 listed GO mean log2FC 柱状图

```r
# ❌ BAD: 把所有 GO 基因 union 成一个 metastasis 基因集再打一个分数
# ❌ BAD: clusterProfiler::enrichGO 全库 BP 冒充“列出的通路”
# ✅ GOOD: for (go in go_ids) score_i <- ssgsea_or_mean_z(expr, genes_of(go))
```
