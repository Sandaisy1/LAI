---
description: TG BRCA RNA-seq：六组比较 + 列出 GO 通路的表达（p0.05/p0.01 分层）
globs: "**/*.{R,r,Rmd}"
alwaysApply: true
---

# TG BRCA RNA-seq：按列出的 GO 通路分析表达

写或改本仓库 R 分析时必须按本规则。输入是 Cuffdiff，不是 Excel。
不要用自选通路上的单独超几何检验替代全库 GO 的 p.adjust。

## 数据

- 目录：`E:/R/TG_BRCA/TG`（脚本与结果同目录；缺失则回退仓库根）
- 首选 `genes.read_group_tracking`，否则 `genes.count_tracking` / `genes.fpkm_tracking`
- 通路列表：`metastasis_custom_genes.txt`（GO 名称 + GO 号，不是基因符号）
- 四个样品独立保留：`NTC_rep0`、`NTC_rep1`、`TG_sh1`、`TG_sh5`
- 物种：人 `org.Hs.eg.db`（GOALL 含子术语）；复合名/`XLOC_` 清洗为一个官方符号

## 预处理（四个样品一起做一次，再做六组比较）

1. 过滤低表达  2. 有 count 用 DESeq2 size factor；仅 FPKM 则 log2 后分位数标准化
3. 不要用未过滤、未标准化的原始值算 FC

## 六组比较（每组单独出表、单独作图）

禁止把 `NTC_rep0` 与 `NTC_rep1` 悄悄合并，除非该组设计明确要求。

1. 四个 1-vs-1：`TG_sh1 vs NTC_rep0`、`TG_sh5 vs NTC_rep0`、`TG_sh1 vs NTC_rep1`、`TG_sh5 vs NTC_rep1`
2. `mean(TG_sh1, TG_sh5)` vs `mean(NTC_rep0, NTC_rep1)`
3. 先做 sh1/sh5 vs `NTC_rep0`，再取共同上调
4. 先做 sh1/sh5 vs `NTC_rep1`，再取共同上调
5. `mean(TG_sh1, TG_sh5)` vs `NTC_rep0`（不要混入 NTC_rep1）
6. `mean(TG_sh1, TG_sh5)` vs `NTC_rep1`（不要混入 NTC_rep0）

脚本分工：1–4 在 `TG_RNAseq_pipeline.R`；5–6 在 `TG_RNAseq_TGsh_mean_vs_NTC_reps.R`。

## 五套分层 + 图

「p 值小于 0.05/0.01」按 **p < 0.05**、**p < 0.01**（用 `pvalue`，不用 padj）。不能估 p 时不要伪造，仍按 FC/排名分层并写日志。

1–2. `p<0.05` 上调 FC ≥ 1/1.25/1.5/2 与 top 50–300
3–4. `p<0.01` 同样两套
5. AllDE：上调+下调（能估 p 时 `p<0.05`）

每个非空子集：差异表、火山图、热图。气泡图：先 `enrichGO` 全库（BP/MF/CC，BH 全库校正），再抽出列出 GO 的 GeneRatio / p.adjust / Count，**不要**在自选通路上重新 `p.adjust`。只画 p.adjust 排名前 20；y 轴按 GeneRatio 从大到小，最大在最上面。全库表在 `GO/`（`ORA_` 前缀），抽出结果在 `CustomGO/`。

## 通路表达

每条列出的 GO 单独打分（ssGSEA 或 mean z），不要把所有 GO 并成一个基因集。结果在 `00_PathwayExpression/`。不要为凸显通路去改全基因组 p 值。

```r
# ❌ BAD: 只在自选 GO 上做超几何并重新 BH，导致 mitochondrion organization 的 p.adjust 虚高
# ✅ GOOD: enrichGO 全库 → 抽出列出 GO 的 GeneRatio/p.adjust/Count → 前20；GeneRatio 最大在最上面
```
