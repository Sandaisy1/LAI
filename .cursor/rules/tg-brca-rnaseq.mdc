---
description: TG BRCA RNA-seq：六组比较 + FoldChange 四档 + 上调排名七档 + 列出 GO 表达
globs: "**/*.{R,r,Rmd}"
alwaysApply: true
---

# TG BRCA RNA-seq：按列出的 GO 通路分析表达

写或改本仓库 R 分析时必须按本规则。输入是 Cuffdiff，不是 Excel。
不要用自选通路上的单独超几何检验替代全库 GO 的 p.adjust。

## 数据

- 目录：`E:/R/TG_BRCA/TG`（脚本与结果同目录；缺失则回退仓库根）
- 首选 `genes.read_group_tracking` 的 **FPKM**（Cuffdiff 已标准化）；不要用 `raw_frags` 再做 DESeq2
- 否则 `genes.fpkm_tracking`
- 通路列表：`metastasis_custom_genes.txt`（GO 名称 + GO 号，不是基因符号）
- 四个样品独立保留：`NTC_rep0`、`NTC_rep1`、`TG_sh1`、`TG_sh5`
- 物种：人 `org.Hs.eg.db`（GOALL 含子术语）；复合名/`XLOC_` 清洗为一个官方符号

## 预处理（四个样品一起做一次，再做六组比较）

1. 过滤低表达  2. 用 Cuffdiff FPKM，只做 log2(x+1)，不再做 DESeq2 size factor / 分位数标准化
3. 不要用未过滤的 raw_frags 算 FC

## 六组比较（每组单独出表、单独作图）

禁止把 `NTC_rep0` 与 `NTC_rep1` 悄悄合并，除非该组设计明确要求。

1. 四个 1-vs-1：`TG_sh1 vs NTC_rep0`、`TG_sh5 vs NTC_rep0`、`TG_sh1 vs NTC_rep1`、`TG_sh5 vs NTC_rep1`
2. `mean(TG_sh1, TG_sh5)` vs `mean(NTC_rep0, NTC_rep1)`
3. 先做 sh1/sh5 vs `NTC_rep0`，再取共同上调
4. 先做 sh1/sh5 vs `NTC_rep1`，再取共同上调
5. `mean(TG_sh1, TG_sh5)` vs `NTC_rep0`（不要混入 NTC_rep1）
6. `mean(TG_sh1, TG_sh5)` vs `NTC_rep1`（不要混入 NTC_rep0）

脚本分工：1–4 在 `TG_RNAseq_pipeline.R`；5–6 在 `TG_RNAseq_TGsh_mean_vs_NTC_reps.R`。

## 两套分层 + 图

每组比较都跑下面两套，**每档单独出表、单独作图**。分层**不用 p**：不按 p 过滤，也不伪造 p。不要合并阈值、不要再套一层 p 目录。

1. **FoldChange** 四组：上调 FC ≥ 1 / 1.25 / 1.5 / 2 → `FoldChange/FC_1` 等
2. **上调排名** 七组：top 50 / 75 / 100 / 150 / 200 / 250 / 300 → `TopRank/top50` 等

不要再跑 p<0.01、AllDE/UpDE/DownDE。

每个非空子集：差异表、火山图、热图。最终气泡图只保留 p.adjust < 0.2，再按 GeneRatio 取前 15 与前 20。enrichGO 用 minGSSize=1，很细的通路也测。全库表在 `GO/`（含 BP/CC/MF 柱状图，横轴 -lgP 与 Count 各一套），抽出结果在 `CustomGO/`（列出通路同样出 BP/CC/MF 柱状图，含全部列出通路）。气泡大小/坐标字体写在 `TG_RNAseq_pipeline.R` 开头；只重画用 `options(tg.rnaseq.restyle_only = TRUE)`。

## 通路表达

每条列出的 GO 单独打分：同时算 mean z，有 GSVA 再算 ssGSEA。全样品热图在 `00_PathwayExpression/`。每个比较另出 `PathwayScore/` 气泡图（分数差）。不要为凸显通路去改全基因组 p 值。

```r
# ❌ BAD: 只在自选 GO 上做超几何并重新 BH，导致 mitochondrion organization 的 p.adjust 虚高
# ✅ GOOD: sig <- hit[p.adjust < 0.2]; for (n in c(15, 20)) plot(head(sig[order(-GeneRatio)], n))
```
