---
description: TG_sh1 vs NTC 细胞 RNA-seq：优先用 Cuffdiff 原始文件重建表达矩阵，再做差异基因、火山图与 GO
globs: "**/*.{R,r,Rmd}"
alwaysApply: true
---

# TG_sh1 vs NTC（Cuffdiff / 细胞 RNA-seq）

数据与 R 脚本目录：`E:/R/TG_BRCA/TG`（Windows 下等价于 `E:\R\TG_BRCA\TG`）。
比较：**TG_sh1（处理）相对 NTC（对照）**。物种按人源细胞用 `org.Hs.eg.db`。

**不要直接读 Excel** `shTG(20220723)`。它只是 `gene_exp.diff` 的拷贝。优先用 Cuffdiff 文本文件重建矩阵并分析。没有 FASTQ/BAM 时不要声称能从测序 reads 重跑比对。

## 输入文件（只用这些）

| 文件 | 用途 |
|------|------|
| `genes.read_group_tracking` | 每基因 × 每 replicate 的 `raw_frags` / FPKM；重建样本级表达矩阵 |
| `read_groups.info` | 样本、condition、replicate 对应 |
| `genes.fpkm_tracking` | `tracking_id` → `gene_short_name` |
| `gene_exp.diff` | Cuffdiff 已有 DE（火山图备选）；列 `gene`、`log2(fold_change)`、`q_value`、`status` |
| `genes.count_tracking` | 可选；条件/重复计数 |

Excel 仅在上述 `.diff` / `.tracking` 缺失时作为 fallback。

## 正确流程

1. 用 `genes.read_group_tracking` 透视成长表→矩阵：行=基因，列=`condition_replicate`。计数用 `raw_frags`（取整后给 DESeq2）；FPKM 只用于展示，不做 DESeq2。
2. 每组 ≥2 个 replicate 时用 **DESeq2**，`condition` 以 NTC 为参考，contrast 为 TG_sh1 vs NTC。每组仅 1 个样本时不要强行 DESeq2，改用 `gene_exp.diff`。
3. 火山图：x = `log2FoldChange`，y = `-log10(padj)`；显著：`padj < 0.05` 且 `|log2FC| > 1`；只保留 `status == "OK"`。
4. GO：显著基因 `SYMBOL` → Entrez，`clusterProfiler::enrichGO`（BP，可加 MF/CC）；Cuffdiff `gene` 列按逗号拆开，丢掉 `-`。

```r
# ❌ BAD：只读 Excel 当“表达矩阵”
deg <- readxl::read_excel("shTG(20220723)")

# ✅ GOOD：从 tracking 重建样本矩阵，再 DESeq2（或回退 gene_exp.diff）
rg <- data.table::fread("genes.read_group_tracking")
rg[, sample := paste(condition, replicate, sep = "_")]
counts <- data.table::dcast(rg, tracking_id ~ sample, value.var = "raw_frags")
```
