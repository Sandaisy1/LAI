---
description: TG BRCA 蛋白共同序列 motif 发现、显著性与序列 logo
globs: "**/*motif*.{R,r}"
alwaysApply: true
---

# TG BRCA：蛋白共同序列（motif）

对给定人源蛋白找 **3–5 条** 共同短序列，必须有真实显著性，并用 bits 序列 logo 出图。不要套用 RNA-seq 的六组比较 / FC / topN。

## 数据

- 脚本：`TG_protein_motif_pipeline.R`；基因表：`TG_protein_motif_genes.txt`
- 数据与结果目录：`E:/R/Protein`（可用环境变量 `TG_PROTEIN_DIR` 覆盖；该目录不存在则创建，仍失败再回退当前工作目录）
- 本地序列覆盖：`E:/R/Protein/TG_protein_motif_sequences.fasta`；结果：`E:/R/Protein/results/`
- 物种：人；序列来自 UniProt Swiss-Prot **reviewed + canonical**（每基因一条）
- 不要用 Cuffdiff / FPKM 当蛋白序列；拉不到序列的基因记入日志，不要丢弃整次分析

## 分析

1. 先拉序列，再 de novo：每条蛋白对每条 motif **至少 1 个位点**（禁止 0 次）；**允许个别蛋白有多个不重叠位点**（额外位点 LLR ≥ max(0, 0.5×该蛋白最佳位点)）。禁止把不同家族硬 MSA 成一张图当“共同序列”。某宽度下只要有蛋白短于该宽度，整档丢弃
2. 宽度扫描 **6–21 aa**（每个整数都扫）。**每个宽度先独立搜选 8 条**（5–10 范围内；该宽度内遮盖已找到位点后再找下一条），再把所有宽度的候选放在一起。**全局得分 = 宽度归一化保守性 = 总 IC / 宽度**（bits/位点；不要用总 IC，也不要用 √宽度折中，更不要按宽度限额或短/中/长交错入选）。PWM 相关 > 0.85 只去掉同一图案的重复，留下更保守的那条。然后按该得分降序取前五（能估计 p 时先滤 **p < 0.01**）
3. 显著性：组成保留重排得到零分布，**p 用单侧 z 检验**（真实 LLR 相对重排均值/标准差）；禁止再用 (1+n_ge)/(1+N) 当展示 p（那会让所有过阈 motif 都变成同一个下限）。能估计时先滤 **p < 0.01**，再按全局得分取前五。`shuffle_floor_p` 只作对照，不作筛选
4. 不能估计 p 时不要伪造；仍出 logo 并在日志写明
5. 只保留过阈值的 motif；不足 3 条就如实少报，不要凑数。不要按“逐轮发现顺序”当排名

## 每条非空 motif 必须出

命中表（基因、UniProt、起止、匹配序列、LLR、p/E）、PWM、对齐 FASTA、**ggseqlogo**（Y 轴 bits 0–4，X 轴 1..w，`col_scheme = "chemistry"`）。文件名与标题带 `motif1`…`motif5` 和 p/E。

## 区域导出与突变方案（新脚本，不改原流程）

`TG_protein_motif_regions_mutations.R` 只读 `E:/R/Protein/results/` 的 hits / PWM / FASTA。不要为加这两项去改 `TG_protein_motif_pipeline.R`。

1. 每个蛋白的 motif 起止、侧翼、区域图 + Excel（`results/regions_mutations/`）
2. 突变方案：默认焦点 `RPL9` / `RBP4` / `ITGAV` / `ITGA2`（`TG_MOTIF_FOCUS_GENES` 可改）；按 PWM 高 IC 位做 C→S、电荷翻转或 Ala scan，每 motif 给出 P1 核心三联

```r
# ❌ BAD: 46 条蛋白强制 MSA 只出一张 logo；或编造 p
# ❌ BAD: 把火山图 / GSEA / FC 分层套到 motif
# ❌ BAD: 为导出区域 / 突变方案去改 TG_protein_motif_pipeline.R
# ✅ GOOD: 3–5 条 motif 各自目录；每条有 hits + bits logo + 真实 p/E
# ✅ GOOD: 区域图 / 突变 Excel 放新脚本，读已有 results/
```
