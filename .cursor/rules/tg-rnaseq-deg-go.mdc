---
description: NTC / TG_sh1 / TG_sh5 细胞 RNA-seq：从 Cuffdiff 重建矩阵，做两两 vs NTC 以及 sh1+sh5 平均 vs NTC 的差异基因、火山图与 GO
globs: "**/*.{R,r,Rmd}"
alwaysApply: true
---

# TG knockdown vs NTC（Cuffdiff / 细胞 RNA-seq）

数据与 R 脚本目录：`E:/R/TG_BRCA/TG`。组别：**NTC（对照）、TG_sh1、TG_sh5**。物种：`org.Hs.eg.db`。
Excel `shTG(20220723)` **只有 NTC vs TG_sh1**，不能做 TG_sh5 或合并分析。优先 `genes.read_group_tracking` 重建矩阵。没有 FASTQ/BAM 时不要声称能重跑比对。

## 必须做的两种分析（都要 DEG + 火山图 + GO）

1. **分别比较**：`TG_sh1 vs NTC`，以及 `TG_sh5 vs NTC`。各出一套结果，禁止只做其中一组。
2. **合并平均**：每个基因 `TG_avg = (mean(TG_sh1) + mean(TG_sh5)) / 2`（两个 shRNA 等权，不按样本数加权）。差异检验用两列 `TG_sh1_mean`、`TG_sh5_mean` 对 NTC 各重复。禁止平均成 1 列再当单样本做 DESeq2；Excel 不能做本分析。

```r
# ❌ BAD：只读 Excel（没有 TG_sh5）；或只做 sh1
deg <- readxl::read_excel("shTG(20220723)")

# ✅ GOOD：tracking 重建后，两种分析都跑
# 1) TG_sh1 vs NTC；TG_sh5 vs NTC
# 2) TG_avg <- (rowMeans(sh1) + rowMeans(sh5)) / 2
#    DESeq2 列：NTC 重复 + TG_sh1_mean + TG_sh5_mean
```

## 输入与阈值

- `genes.read_group_tracking`：样本级 `raw_frags` / FPKM；`read_groups.info`；`genes.fpkm_tracking`（symbol）
- `gene_exp.diff` 仅作 pairwise 回退（Excel 同样只能回退 sh1 vs NTC）
- 计数给 DESeq2，NTC 为参考；每组 <2 重复则该对比回退 Cuffdiff/简单检验
- 火山图：x=`log2FC`，y=`-log10(padj)`；点颜色仍可标上/下调
- **GO + KEGG + 通路富集只做上调基因**，禁止下调、禁止上调+下调合并。每个基因集都要：`enrichGO`（BP/MF/CC）、`enrichKEGG`（hsa）、**Reactome**（`enrichPathway`）和 **WikiPathways**（`enrichWP`）。输出写在同一目录（`GO_*`、`KEGG_*`、`Reactome_*`、`WikiPathways_*`）。线性 FC 分四组各自富集，禁止合并：FC≥1、1.25、1.5、2。另需 `padj<0.05`（无 padj 时只用 FC）。
- 结果分目录：`results/pairwise/TG_sh1_vs_NTC/`、`.../TG_sh5_vs_NTC/`、`results/pooled_avg/TG_sh1_sh5_avg_vs_NTC/`

## 当前任务：两组共同上调基因 GO + KEGG + 通路（`scripts/tg_common_up_go.R`）

先单独比较 `TG_sh1 vs NTC` 和 `TG_sh5 vs NTC`，再取**两侧都上调**的基因做 GO、KEGG、Reactome、WikiPathways。禁止对单组代替共同基因；禁止把两组上调基因取并集。

1. **按线性 FC**（两侧都达标，用 `min(FC_sh1, FC_sh5)`）：FC≥1、1.25、1.5、2 四组各自富集。
2. **按排名交集**：各组上调前 50 / 75 / 100 / 150 / 200 / 250 / 300 的交集，各自富集。禁止不同 N 合并。

通路层文件：`Reactome_<tag>.tsv`、`WikiPathways_<tag>.tsv` 及对应 dotplot/barplot。共享函数：`scripts/kegg_enrich_helpers.R` 中的 `save_kegg_and_pathways()`。
