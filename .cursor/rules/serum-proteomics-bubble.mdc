---
description: 血清蛋白质组：两样品平均丰度排名气泡图（等大）
globs: "**/serum_proteomics*.{R,r,py},**/run_serum_proteomics*.R,**/*pg_matrix*,**/*pr_matrix*"
alwaysApply: false
---

# 血清蛋白丰度排名气泡图

写或改血清蛋白质组脚本时按本规则。输入是 **DIA-NN** 矩阵，不是 Cuffdiff。RNA-seq 仍走 `tg-brca-rnaseq.mdc`。

## 数据

- 目录：`E:/天府/实验管理/课题/赵章寻/血清蛋白质组学`
- 首选 `report.pg_matrix` / `report.pg_matrix.tsv`；过少则 `report.pr_matrix` 按 `Protein.Group` 取肽段中位数
- 当前两列样品 `GP_WJZ_11`、`GP_WJZ_18`：**先对两列取平均，再按平均丰度排名**。不要做成两组比较的 FC 气泡图，也不要靠气泡大小编码丰度
- 去免疫球蛋白：另跑 `serum_proteomics_ranked_bubble_no_Ig.R`（不要改原脚本）。去掉 IGH/IGK/IGL/JCHAIN 等后再平均、再排名；结果在 `results/serum_proteomics_bubble_no_Ig/`，并写出 `removed_immunoglobulins.csv`。不要把 IGSF 等超家族当抗体去掉

## 预处理

1. 过滤：至少 50% 样品有非零强度
2. `log2(x+1)` 后按样品中位数中心化，再对两样品取平均，得到每个蛋白的丰度值
3. 不要用未标准化的原始强度排名

## 图

横轴 = **丰度排名**（1 = 最高），纵轴 = **蛋白丰度值**（两样品 log2 平均），**气泡大小一致**。默认 top20 / top30 / top50 以及全蛋白各出一张。点旁可标基因名，不要用 FC 或组别当坐标。

```r
# ❌ BAD: x=log2FC、点大小随丰度变；把两样品当两组做 1-vs-1
# ✅ GOOD: abundance = mean(样品1, 样品2) → rank → geom_point(size 固定)
```
