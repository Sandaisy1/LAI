---
description: TG 敲低细胞 RNA-seq 必须按 NTC/TG_sh1/TG_sh5 三组比较与两套上调筛选策略分析
globs: "**/*.{R,r,Rmd}"
alwaysApply: true
---

# TG-BRCA 细胞 RNA-seq 分析约定

数据与 R 脚本目录：`E:/R/TG_BRCA/TG`（仓库内默认 `data/`）。组别固定为 **NTC**（对照）、**TG_sh1**、**TG_sh5**。Excel `shTG(20220723)` 是 Cuffdiff 矩阵（常见列：`gene`、`sample_1`、`sample_2`、`value_1`、`value_2`、`log2(fold_change)`、`p_value`、`q_value`），图中往往只有 NTC vs TG_sh1。缺 TG_sh5 时必须从同目录 `genes.fpkm_tracking` / `gene_exp.diff` 补齐三组 FPKM，禁止只分析 Excel 里出现的两组。

## 必须完成的三组比较（缺一不可）

1. **成对差异**：分别做 `TG_sh1 vs NTC` 与 `TG_sh5 vs NTC`。
2. **合并平均**：两敲低组 FPKM 先取均值，再 vs NTC。
3. **共同上调**：在第 1 组两套结果中取上调基因**交集**（不是并集）。

```r
# ❌ BAD：只做 Excel 中的 NTC vs TG_sh1；或把 sh1/sh5 基因取并集
# ✅ GOOD
sh1 <- deg(fpkm, treat = "TG_sh1", ctrl = "NTC")
sh5 <- deg(fpkm, treat = "TG_sh5", ctrl = "NTC")
avg <- deg(data.frame(NTC = fpkm$NTC, SH = rowMeans(fpkm[, c("TG_sh1", "TG_sh5")])),
           treat = "SH", ctrl = "NTC")
common_up <- intersect(up_genes(sh1), up_genes(sh5))
```

## 两套筛选（对上面每一组都要跑；只做上调）

线性 FC = treat / ctrl（或 `2^log2FC`）。“1 倍 / 1.25 倍 / 1.5 倍 / 2 倍”指 **FC≥1, 1.25, 1.5, 2**，不要把“2 倍”写成 `|log2FC|≥2`（那是 4 倍）。FC≥1 即全部上调（log2FC≥0）。

- **FC 阈值**：FC ≥ 1、1.25、1.5、2 各出一套结果。
- **上调排名**：按 log2FC 降序取前 50、75、100、150、200、250、300。

每一套都要写差异表，并做火山图、热图（列必须含 NTC、TG_sh1、TG_sh5）、GO、通路富集、KEGG、GSEA。
